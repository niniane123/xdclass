包将有联系的模块组织在一起，即放在同一文件夹下，并且在这个文件夹中创建一个名字为__init_.py的文件，那么这个文件夹就成为包

制作包
 一个包内都是存放的有联系的模块。注意，新建包之后，包内部会自动创建__init__.py文件，这个文件按控制指着包的导入行为

导入包
    导入方式1：import 包名.模块名
    调用方式1：包名.模块名.目标

    导入方式2：必须在__init__.py文件中添加__all__=[]，控制允许导入的模块列表
          from 包名 import *
    调用方式2：
        模块名.目标函数


